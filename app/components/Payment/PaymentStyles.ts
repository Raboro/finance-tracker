import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        borderWidth: 3,
        margin: '3%',
        borderRadius: 10,
        textAlign: 'center',
        marginHorizontal: '20%'
    }
});